apiVersion: apps/v1
kind: Deployment
metadata:
  name: gameday-workflow-workflow-notification
  namespace: gameday-workflow
  labels:
    app: gameday-workflow-workflow-notification
spec:
  replicas: 1
  selector:
    matchLabels:
      app: gameday-workflow-workflow-notification
  template:
    metadata:
      labels:
        app: gameday-workflow-workflow-notification
    spec:
      containers:
        - name: gameday-workflow-workflow-notification
          # CIのdeploy.ymlが `kubectl set image` で実際のECRイメージに差し替える。初回applyのみこのタグが使われる。
          image: gameday-workflow-workflow-notification:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8003
              name: http
          env:
            - name: PORT
              value: "8003"
            - name: RUST_LOG
              value: "info"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: gameday-workflow-workflow-notification-secrets
                  key: database-url
            - name: OTEL_SERVICE_NAME
              value: "gameday-workflow-workflow-notification"
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: "http://gameday-workflow-nrdot-collector.gameday-workflow.svc.cluster.local:4318"
            - name: OTEL_EXPORTER_OTLP_PROTOCOL
              value: "http/protobuf"
            - name: OTEL_RESOURCE_ATTRIBUTES
              value: "service.namespace=gameday-workflow,deployment.environment=production"
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /api/v1/health
              port: 8003
            initialDelaySeconds: 15
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /api/v1/health
              port: 8003
            initialDelaySeconds: 5
            periodSeconds: 5
